"""Tournament result analyzer.

Turns the per-node CSV dumps of ``benchmarks/blink/tournament_nb.c`` into
per-node / per-round / overall bandwidth & latency statistics, annotated with
topology distance, and renders text + plots. See ``PLAN_RESULT_ANALYZER.md``.
"""
